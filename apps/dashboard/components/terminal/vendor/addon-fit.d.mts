/**
 * The fit addon's whole surface, which really is this small.
 *
 * See `xterm.d.mts` for why these are declared rather than vendored.
 */

export declare class FitAddon {
  /** Resizes the terminal to the pixel size of the element it was opened in. */
  fit(): void;
}
